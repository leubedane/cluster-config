apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    argocd.argoproj.io/sync-options: Validate=false
  name: kong-proxy
spec:
  host: kong-proxy-kong.apps.baloise.dev
  port:
    targetPort: proxy
  tls:
    termination: edge
  to:
    kind: Service
    name: kong-proxy
    weight: 100
  wildcardPolicy: None